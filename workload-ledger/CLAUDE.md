# CLAUDE.md — workload-ledger

Agent instructions live in **AGENTS.md** (cross-tool standard). Read it first.
Append-only run log; the schema is FROZEN — boring + stable is the point. `cargo test --release`; `./target/release/workload-ledger stats`.

@AGENTS.md
