# CLAUDE.md — skill-tuner

Agent instructions live in **AGENTS.md** (cross-tool standard). Read it first.
Nightly transcript→diff-proposal loop. PROPOSALS ONLY, never auto-applies. `python3 skill-tuner.py --dry-run --days 1`.

@AGENTS.md
