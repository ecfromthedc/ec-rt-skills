# CLAUDE.md — curation-ledger

Agent instructions live in **AGENTS.md** (cross-tool standard). Read it first.
Kill-rate metric: generated vs shipped per pipeline/page/week. `cargo test --release`; `./target/release/curation-ledger report`.

@AGENTS.md
